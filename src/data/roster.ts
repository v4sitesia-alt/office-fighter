/** Ids dos lutadores (pasta public/fighters/<id>) na ordem da tela de seleção. */
export const ROSTER = ['edgard', 'santana', 'kevin'];
/** Cenário usado quando o lutador não define o seu. */
export const DEFAULT_STAGE = 'office';
